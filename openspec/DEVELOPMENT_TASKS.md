# DancingPluginSdk Tasks

## Milestone A

1. Stabilize `DancePlugin` and `AudioData` contracts.
2. Add at least one runnable plugin example.
3. Add compatibility matrix for host versions.
4. Release `v1.x` compatible package.
